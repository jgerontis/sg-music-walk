import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'jgerontis.ionic.sgmusicwalk',
  appName: 'SG Music Walk',
  webDir: 'dist',
};

export default config;
